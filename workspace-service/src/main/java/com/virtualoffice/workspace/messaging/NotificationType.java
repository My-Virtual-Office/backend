package com.virtualoffice.workspace.messaging;

// Serialized by name to the notifications exchange; must match a value in notifications-service.
public enum NotificationType {
    WORKSPACE_INVITE
}
